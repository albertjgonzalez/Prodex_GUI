
import firebase from 'firebase';

const singleBeat = (name,file,location)=>{

    
    return (
        //return success or failue
        alert(name)
    )
}

const beatPack = (names,file,location)=>{
    //send beat(s) and info to firebase
    
    return (
        //return success or failue
        alert(names)
    )
}

export { singleBeat,beatPack }